export default function HomePage() {
  return (
    <main>
      <h1>ApexRacers</h1>
      <p>
        Find the car that best fits your pace. ApexRacers aggregates iRacing weekly series
        lap time data and shows where you rank by percentile against the full field — so
        you can pick the car where you are most competitive.
      </p>
      {/* TODO: Add "Sign in with iRacing" OAuth button */}
      {/* TODO: Show featured active series cards linking to /series */}
    </main>
  );
}
