export default function RecommendationsPage() {
  // TODO: Resolve weekId from query string or a week-picker component
  // TODO: Call api.getRecommendations(weekId) for the authenticated user
  // TODO: Render ranked CarRecommendation list: rank, car name, percentile, sample size

  return (
    <main>
      <h1>My Car Recommendations</h1>
      <p>
        <em>Personalised recommendations — not yet implemented.</em>
      </p>
    </main>
  );
}
