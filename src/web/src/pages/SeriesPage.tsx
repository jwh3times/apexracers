export default function SeriesPage() {
  // TODO: Call api.getSeries() on mount and store results in state
  // TODO: Render a card or table row per Series with a link to the current week's detail page:
  //       /series/:seriesId/weeks/:weekId

  return (
    <main>
      <h1>Active Series</h1>
      <p>
        <em>Loading series data — not yet implemented.</em>
      </p>
    </main>
  );
}
